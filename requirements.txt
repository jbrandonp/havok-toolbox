# HAVOK Toolbox v0.8.0
# Core install: pip install -e .
# Full: pip install -e ".[all]"
# See pyproject.toml for categorized optional dependencies (viz, export, detection, cli, etc.)

numpy>=1.21
scipy>=1.7
scikit-learn>=1.0

# Recommended for full use (or use extras above)
pandas>=1.3
pyyaml>=6.0
click>=8.0
plotly>=5.0
streamlit>=1.25
ruptures>=1.1

# Optional extras (see pyproject)
# pip install -e ".[streaming]" etc.
